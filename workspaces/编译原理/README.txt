示例项目：编译原理
==================

这是按「可读项目名」建的工作区示例。

用法：
- 在浏览器打开 /w/编译原理，或在前端输入「编译原理」点「打开项目」
- 把剧本/文档放到 input/ 下（如 input/编译原理.docx）
- 生成的卡片会出现在 output/

目录结构：
  编译原理/
    input/     ← 上传或放入原始文档
    output/    ← 生成的卡片、trainset、优化结果等