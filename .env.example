# ===========================================
# EduFlow 配置文件示例
# 复制此文件为 .env 并填写你的配置
# ===========================================

# -------------------------------------------
# LLM API 配置（必填其一）
# -------------------------------------------
# DeepSeek API 密钥（用户未在「设置」中填写时，若选 DeepSeek 则使用此项）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 豆包 API 密钥：选豆包时使用；若配置此项，用户未设置 Key 时将自动使用此项作为默认（不向用户暴露）
# LLM_API_KEY=your_doubao_api_key_here

# 使用的模型（可选，默认 deepseek-chat）
# DEEPSEEK_MODEL=deepseek-chat

# -------------------------------------------
# 智慧树平台配置（注入功能需要）
# -------------------------------------------
# 平台基础URL（通常不需要修改）
# PLATFORM_BASE_URL=https://cloudapi.polymas.com

# 登录Cookie（从浏览器开发者工具获取）
PLATFORM_COOKIE=your_cookie_here

# JWT Token（从浏览器开发者工具获取）
PLATFORM_AUTHORIZATION=Bearer your_jwt_token_here

# 课程ID
PLATFORM_COURSE_ID=your_course_id

# 训练任务ID
PLATFORM_TRAIN_TASK_ID=your_train_task_id

# 起始节点ID（"训练开始"节点）
PLATFORM_START_NODE_ID=your_start_node_id

# 结束节点ID（"训练结束"节点）
PLATFORM_END_NODE_ID=your_end_node_id

# -------------------------------------------
# 卡片默认配置（可选）
# -------------------------------------------
# AI模型ID（平台上的模型选择）
# CARD_MODEL_ID=doubao-seed-1.6

# 虚拟训练官名称
# CARD_TRAINER_NAME=ai

# 默认交互轮次
# CARD_INTERACTION_ROUNDS=5

# 历史记录数量（-1表示全部）
# CARD_HISTORY_NUM=-1

# -------------------------------------------
# 用户认证（可选，启用后需登录才能使用工作区）
# -------------------------------------------
# 生产环境：设为 production 后，必须配置 JWT_SECRET 且不能为默认值，否则服务拒绝启动
# EDUFLOW_ENV=production
# JWT 密钥（生产环境必填：设为随机长字符串）
# JWT_SECRET=your_random_secret_here
